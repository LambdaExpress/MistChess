# MistChess 第四阶段开发规格

## 1. 目标

第四阶段交付以下能力：

1. 每次服务端确认的普通走棋在红黑双方客户端各播放一次相同的走棋声，不再由落子方本地单独播放。
2. 增加仅针对自己上一手棋的悔棋请求，并保证请求、对手走棋、超时和终局之间的并发结果唯一且可恢复。
3. 将悔棋、求和等双方请求放到棋盘中央显著提示，使手机竖屏玩家无需滚动到棋盘下方即可看到并处理。
4. 从实时对局页移除 `LIVE GAME · {ruleVersion}`、实时连接状态和局面版本等内部状态展示。
5. 己方回合时让棋盘边框持续做低频呼吸闪烁；对方回合、等待命令和终局时停止。
6. 在普通历史回放和管理员回放中，将“查看哪一方的信息”与“棋盘朝向”拆成两组互不影响的控制。
7. 全局视野继续展示完整局面，同时用红色、黑色半透明区域标明红方和黑方在该历史时刻各自看不到的位置。
8. 吃子时在双方客户端播放显著的中文“吃”语音，并保留与普通落子明显不同的重击听感。
9. 实时计时对局将对手计时器放在棋盘上方，将自己的计时器放在棋盘下方，不再固定按黑方、红方排列。
10. 精简所有历史回放入口的步进控制，只保留上一步、下一步、当前步数和总步数。
11. 历史回放向前步进时播放对应的走棋、吃子和终局声音。

## 2. 现有基础与兼容约束

- 第三阶段已经将普通走棋和吃子映射为独立音频资源，并保留音效开关、音量、浏览器自动播放解锁、权威版本去重和事件优先级。第四阶段沿用同一个 `AudioService`，不得建立第二套播放状态。
- 实时棋局以服务端 `GameEntity.StateJson`、单调递增的 `GameEntity.Version` 和 `moves` 记录为权威状态。悔棋必须在服务端事务内完成，前端不得自行删除棋子、倒退版本或修改计时。
- 当前求和使用持久化 `DrawOfferEntity` 和 `DrawOfferChanged` 实时通知。悔棋采用相同的服务端权威请求模式，但使用独立实体和契约，不能把两种请求仅靠前端文案区分。
- 当前 `MoveEntity` 保存每手后的局面、计时和终局快照，`GameEntity.InitialStateJson` 保存初始局面。悔棋回退应复用这些快照，并保留被撤销走棋的审计记录。
- `GameBoard` 同时用于实时棋局、普通回放和管理员回放。棋盘坐标变换、棋子、迷雾、候选落点和点击热区必须继续共用同一朝向参数。
- 当前回放把红方、黑方、全局三种投影都保存在 `HistoricalReplayFrameView.Views` 中。第四阶段不得降低红黑视野的隐私边界，也不得从实时玩家接口暴露对手不可见信息。
- 第三阶段手机竖屏棋盘采用 `5 / 6` 比例和棋子内容横向补偿。第四阶段布局改动必须保留该几何、点击位置和桌面、横屏行为。

## 3. 功能规格

### 3.1 双方同步走棋音效

#### 3.1.1 触发来源

普通走棋和吃子声音只由服务端已经提交的权威棋局变化触发：

1. 服务端事务成功提交走棋后，红方和黑方都收到同一权威更新。
2. 两个客户端都根据更新中的权威动作类型播放声音。
3. 落子方收到 HTTP 成功响应和实时推送时，仍只播放一次。
4. 不允许在点击候选落点、发送请求或客户端乐观更新时提前播放。
5. 请求失败、非法走法、陈旧版本、超时前未提交的走法和已撤销走棋均不得播放普通走棋声。

`GameView` 增加不含坐标的权威动作摘要，避免仅根据 `sideToMove` 和吃子数量猜测事件：

```text
lastAction.version   对应 GameEntity.Version
lastAction.kind      move | capture | takebackAccepted
lastAction.actor     red | black
```

终局状态仍由 `GameView.Result` 判定。非吃子终局直接播放终局声音；终局吃子按下一节规定顺序播放“吃”和终局声音。接受悔棋会增加棋局版本，但 `lastAction.kind = takebackAccepted`，不得被误判为一次普通走棋。

#### 3.1.2 去重与优先级

普通权威版本最多播放一个声音，规则如下：

1. 非终局吃子播放中文“吃”语音，不叠加普通走棋声。
2. 非吃子的普通走棋播放走棋声。
3. 非吃子终局只播放终局胜、负、和声音。
4. 吃子直接造成终局时，先播放中文“吃”语音，再在语音主要瞬态结束后顺序播放终局声音；不得同时重叠播放，也不得插入普通走棋声。
5. 悔棋成功本阶段不新增声音。

去重键继续使用 `gameId + version + event`。终局吃子的 `capture` 与终局事件分别去重，因此 HTTP 响应和实时推送顺序互换时仍各播放一次。初次进入已经进行中的棋局、刷新、断线重连、重复响应和取得相同或更旧版本时，不补播历史走棋、吃子或终局声音。

#### 3.1.3 验收标准

- 玩家 A 普通走棋后，A 与 B 各听到一次相同走棋声。
- 玩家 B 普通走棋后，A 与 B 各听到一次相同走棋声。
- 任一方吃子后，双方只听到一次“吃”语音，不叠加普通走棋声。
- HTTP 响应与实时推送顺序互换时，落子方都不会重复播放。
- 接受悔棋造成版本增加和行棋方变化时，双方不播放走棋声或吃子声。
- 重连取得当前局面时不补播断线期间的历史声音。

### 3.2 悔棋请求

#### 3.2.1 可发起窗口

悔棋严格限定为“撤销自己刚完成的上一手”：

- 棋局必须仍为 `Playing`。
- 棋局至少有一条未撤销走棋。
- 最新一条未撤销走棋必须由请求者完成。
- 当前必须轮到对方行棋，即 `game.SideToMove != requesterSide`。
- 请求必须绑定该最新走棋的 `moveId`、`ply` 和发起时的 `expectedVersion`。
- 同一条走棋最多发起一次悔棋请求。对方拒绝后，请求者不能针对同一手重复发送。
- 对方一旦成功提交下一手棋，上一手的悔棋窗口永久关闭；不能再跨两手或跨回合悔棋。
- 造成终局的走棋不能请求悔棋，因为走棋提交后棋局已经不再处于 `Playing`。

前端只在满足以上条件时显示“请求悔棋”。服务端必须独立重复校验，不能依赖按钮是否可见。

#### 3.2.2 请求与响应

- 发起成功后，请求者看到“已请求撤销上一手，等待对方回应”。
- 对方在棋盘中央看到“对手请求悔棋”，并看到被请求撤销的手数及“同意”“拒绝”按钮。
- 请求者不能同意或拒绝自己的请求。
- 对方拒绝后，请求关闭，棋局和计时不回退。
- 对方同意后，只撤销请求者最新的一手，行棋权回到请求者；不能一次撤销双方各一手，也不能选择撤销手数。
- 本阶段不提供请求者主动取消接口。请求可因拒绝、接受、对方走棋、超时或终局而关闭。

#### 3.2.3 对方直接走棋

悔棋请求不暂停棋局，也不强制对方先点击“拒绝”：

- 对方仍可以正常走棋。
- 对方走棋事务提交时，必须在同一事务中把待处理悔棋请求改为 `Withdrawn`。
- 走棋提交后，再到达的同意请求返回 `409 TAKEBACK_WINDOW_CLOSED`，不得回退对方已经提交的棋。
- 同意悔棋事务先提交时，原走棋版本立即失效；对方基于旧版本提交的走法返回 `409 STALE_VERSION` 并刷新棋局。
- 并发结果以服务端串行化事务的提交顺序为准，任何顺序下都只能得到一个权威结果。

#### 3.2.4 与求和及其他命令的关系

同一棋局同一时刻只允许一个待处理的双方协商请求：

- 已有待处理求和时，发起悔棋返回 `409 NEGOTIATION_PENDING`。
- 已有待处理悔棋时，发起求和返回 `409 NEGOTIATION_PENDING`。
- 任一合法走棋都会撤销当前待处理的求和或悔棋请求。
- 认输、超时、管理员判负或其他终局会把待处理请求改为 `Withdrawn`。
- 接受求和会结束棋局；接受悔棋只回退一手并继续棋局。

#### 3.2.5 回退规则

接受悔棋必须在一个可串行化数据库事务中完成：

1. 锁定棋局、待处理悔棋请求和绑定的最新未撤销走棋。
2. 结算当前对方回合已经流逝的时间；如果结算结果已经超时，则按超时结束棋局并撤销悔棋请求。
3. 再次确认棋局版本、最新有效走棋和请求绑定的 `moveId` 未发生变化。
4. 将目标走棋标记为已撤销，保留原始走棋数据用于审计，不做物理删除。
5. 使用上一条未撤销走棋的 `StateAfterJson` 恢复局面；撤销第一手时使用 `GameEntity.InitialStateJson`。
6. 从恢复后的领域状态设置 `StateJson` 和 `SideToMove`，清除不再适用的结果字段，棋局继续保持 `Playing`。
7. `GameEntity.Version` 在当前值上加一，绝不能倒退到旧版本或改成当前半回合数。
8. 请求状态改为 `Accepted`，记录解决时间和接受后版本。
9. 在事务提交后向双方发送悔棋状态和棋局更新通知。

计时遵循“悔棋不返还思考时间”：

- 发起和处理请求期间时钟继续运行。
- 请求者原走棋已经消耗的总时间不返还。
- 原走棋获得的增量从请求者总时间中撤销，最低截断为零。
- 对方等待和处理请求期间已经消耗的总时间不返还。
- 有单步限时时，请求者重新行棋可用的单步时间为 `max(0, 原单步限时 - 被撤销走棋的 ElapsedMilliseconds)`，避免通过反复悔棋重新获得完整单步时间。
- 无计时棋局只恢复局面和行棋方，不创建计时字段。

接受后，重复的同意请求返回同一权威结果；重复的拒绝请求返回最终请求状态。不得重复撤销走棋或重复增加版本。

#### 3.2.6 历史与评分

- 被撤销走棋不出现在普通历史回放、分享回放、管理员默认回放和总手数中。
- 管理员审计数据可以保留被撤销走棋及关联请求，但普通玩家接口不得返回审计字段。
- 撤销后重新落子可以再次使用同一个 `ply`，有效走棋序列仍必须从 1 连续编号。
- 悔棋只发生在进行中棋局，不触发评分结算。之后的正常终局继续只结算一次。
- 重复局面与无进展计数使用恢复后的 `GameState`，不能保留被撤销走棋产生的历史键或计数。

### 3.3 棋盘中央请求提示

#### 3.3.1 展示范围

实时棋局的以下状态必须在棋盘中央显示请求卡片：

- 收到对手的求和请求。
- 自己已发出求和请求并等待回应。
- 收到对手的悔棋请求。
- 自己已发出悔棋请求并等待回应。

侧栏中的现有求和提示移入棋盘覆盖层。桌面和移动端使用同一组件，例如 `GameNegotiationOverlay`，不得维护两套状态或两套响应按钮。

#### 3.3.2 视觉与交互

- 请求卡片以棋盘容器为定位上下文，固定在棋盘视觉中心，不依赖页面滚动位置。
- 卡片使用高对比背景、阴影和半透明遮罩，宽度不超过棋盘的 82%；在 320 CSS 像素宽视口仍完整显示。
- 收到请求时使用 `role="alertdialog"` 和明确标题，焦点移到首个响应按钮；状态变化通过 `aria-live` 宣告。
- 对手请求卡片提供“同意”和“拒绝”。自己发出的等待卡片不提供伪造的本地取消按钮。
- 终局结果优先于请求卡片。棋局结束、请求被走棋撤销或服务端返回最终状态时立即关闭覆盖层。
- 网络重连后从 `GameView` 恢复待处理状态；不能只依赖曾经收到的 SignalR 事件。
- 请求响应提交期间禁用重复点击，并显示提交中状态。失败后恢复按钮并展示服务端错误。

### 3.4 实时对局页信息精简

从所有视口下的实时 `GamePage` 移除以下可见内容及其占位空间：

```text
LIVE GAME · {ruleVersion}
实时连接中 / 实时同步 / 正在恢复实时连接 / 实时连接中断
局面版本 {version}
```

具体要求：

- 保留“轮到你行棋”“等待红方行棋”“等待黑方行棋”和“棋局结束”主标题。
- SignalR 连接状态和棋局版本继续在内部用于重连、缓存与命令并发控制，不得删除其业务逻辑。
- 对手离线、命令失败和封禁等会影响玩家操作的通知继续显示。
- 如需无障碍提示连接中断，可以使用屏幕阅读器专用的 `aria-live` 区域，但页面上不得保留截图红框中的可见标签、圆点、版本号或空白块。
- 移除内容后收紧头部上下间距，让手机首屏优先展示对手计时器和棋盘。

### 3.5 己方回合棋盘边框提示

当且仅当以下条件同时成立时，棋盘增加 `game-board--my-turn` 状态：

```text
view.status == playing
view.sideToMove == view.perspective
commandPending == false
```

视觉规则：

- 棋盘外框使用 1.4 至 1.8 秒一个周期的低频呼吸动画，在当前深绿色和较亮的金绿色描边、外发光之间平滑变化。
- 不改变棋盘尺寸和布局，不移动棋子，不覆盖候选落点，不拦截点击。
- 对方回合、命令等待、断线导致交互锁定、接受悔棋处理中和终局时立即停止动画。
- 页面切到后台时允许浏览器暂停动画；恢复可见后根据最新权威局面重新判断。
- `prefers-reduced-motion: reduce` 下取消动画，改为稳定的高亮描边和静态外发光。
- 动画不得使用每秒三次以上的高对比闪烁。

### 3.6 视野模式与棋盘朝向解耦

#### 3.6.1 独立状态

普通回放和管理员回放各维护两个正交状态：

```text
visibilityMode = red | black | omniscient
orientation    = red | black
```

- `visibilityMode` 只决定读取 `frame.views.red`、`frame.views.black` 或 `frame.views.omniscient` 中的哪一份棋子和可见区域数据。
- `orientation` 只决定哪一方位于棋盘下方以及所有坐标如何旋转。
- 切换红方、黑方或全局视野时，`orientation` 必须保持原值。
- 切换“红方在下”“黑方在下”时，`visibilityMode`、当前回放帧和播放状态必须保持原值。
- `GameBoard` 增加显式 `orientation` 参数。棋子位置、迷雾方格、选择框、候选落点、点击热区和无障碍坐标描述统一使用该参数，不能继续借用 `GameView.Perspective` 完成回放旋转。
- 实时棋局不新增自由旋转按钮，继续以当前玩家一方在下；本项只修改只读回放入口。

#### 3.6.2 控件与默认值

回放页展示两组独立控件：

1. “信息视野”：红方视野、黑方视野、全局视野。
2. “棋盘朝向”：红方在下、黑方在下；也可以使用一个带明确标签的“翻转棋盘”按钮。

默认规则：

- 普通玩家查看自己的历史时，`visibilityMode` 和 `orientation` 都默认取 `currentPlayerSide`。
- 公开分享回放保持现有默认信息视野；没有当前玩家身份时棋盘默认红方在下。
- 管理员回放默认全局视野、红方在下。
- 手动切换回放帧不得重置 `visibilityMode` 或 `orientation`。

### 3.7 全局视野迷雾标记

全局视野仍使用 `frame.views.omniscient.pieces` 展示完整棋子，但不能再呈现为毫无遮罩的普通全盘：

- 对红方不可见的位置覆盖红色半透明遮罩。
- 对黑方不可见的位置覆盖近黑色半透明遮罩。
- 双方都可见的位置不加遮罩。
- 双方都不可见的位置同时表达两种状态，使用红黑叠层或红黑斜纹；不能只归到其中一方。
- 遮罩必须允许观看者辨认其下的棋子和棋盘线，建议不透明度控制在 `0.20` 至 `0.36`。
- 遮罩不改变 `omniscient` 投影的数据权限，也不影响上一步和下一步操作。
- 全局视野旁增加图例：“红方盲区”“黑方盲区”“双方可见”；双方盲区的组合样式也应可辨认。

渲染时分别从同一帧的 `views.red.visibleSquares` 和 `views.black.visibleSquares` 计算盲区，不能使用棋子是否存在推断可见性。红方视野和黑方视野继续使用现有不透明未知区域，不应用全局半透明样式。

### 3.8 吃子中文语音

#### 3.8.1 资源

新增或替换为以下本地资源：

```text
apps/web/public/audio/capture-chi.ogg
apps/web/public/audio/capture-chi.mp3
```

资源要求：

- 清楚朗读单个中文“吃”字，发音有力度、起音迅速，手机扬声器上也能辨认。
- 将“吃”语音与第三阶段的厚重吃子碰撞声制作成一个最终音频文件，避免运行时同时启动两个音频导致不同步或重复。
- 建议总时长为 300 至 650 毫秒，语音不得被碰撞声覆盖，峰值不得削波。
- 不复制或截取《天天象棋》及其他第三方游戏的语音。素材必须为项目原创、自行录制、合法文本转语音或有明确分发授权，并更新 `apps/web/public/audio/README.md`。
- Ogg 和 MP3 必须来自同一母带，听感和时长一致。

`AudioService` 将 `capture` 显式映射到 `capture-chi`，播放速度固定为 `1`。吃子仍作为一个逻辑事件参与权威版本去重，不能先播放普通走棋声再播放“吃”。

#### 3.8.2 验收标准

- 红方吃子时双方各听到一次清晰“吃”语音。
- 黑方吃子时双方各听到一次清晰“吃”语音。
- 关闭音效或音量为零时无可听输出，重新开启后下一个新事件正常播放。
- Chromium、Firefox 和手机 Chromium 按支持情况选择 Ogg 或 MP3。
- 吃子直接造成终局时，先播放一次“吃”，再顺序播放一次终局声音；两个声音不得重叠，HTTP 与实时推送不得导致任一声音重复。

### 3.9 双方计时器上下布局

#### 3.9.1 排列规则

实时计时棋局不再固定显示“黑方在上、红方在下”的侧栏卡片。计时器按当前玩家身份排列：

```text
棋盘上方：对手计时器
棋盘下方：自己计时器
```

- 红方玩家看到黑方计时器在上、红方计时器在下。
- 黑方玩家看到红方计时器在上、黑方计时器在下。
- 两个计时器都显示方色、我方或对方标签、总剩余时间、当前单步剩余时间和时间不足状态。
- 当前正在计时的一方保留现有活动高亮；低于 10 秒继续使用明显但可读的告警样式。
- 棋局结束后保留双方最终时间，但停止活动动画和本地插值。
- 无计时棋局不渲染空计时器，可以保留紧凑的当前行棋提示。

#### 3.9.2 响应式布局

- 计时器属于棋盘列，不再放在 `.game-sidebar` 内。
- 对手计时器紧邻棋盘上边缘；自己计时器紧邻棋盘下边缘，棋盘图例放在自己计时器之后。
- 手机竖屏使用单行紧凑计时条，每条高度应控制在约 48 至 64 CSS 像素，不导致横向滚动。
- 400 × 845 CSS 像素视口中，不滚动即可看到对手计时器、完整棋盘以及自己的主要剩余时间。
- 桌面布局仍可保留右侧的对局操作和已损失棋子，但计时器不能回到侧栏。
- 计时条不能覆盖棋盘、中央请求卡片或棋盘边框动画。

### 3.10 回放控制精简

#### 3.10.1 生效范围

以下只读回放入口统一使用精简控制：

```text
普通玩家历史回放
公开分享回放
管理员回放
```

回放控制区只保留：

- “上一步”按钮。
- “下一步”按钮。
- 当前步数与总步数，使用紧凑格式，例如 `0 / 36`，并提供可读的“当前第 0 步，共 36 步”无障碍标签。

删除以下内容及其占位空间：

- “当前进度”等重复标题。
- “初始局面”“对手走子或终局事件”、走棋坐标和吃子摘要等当前局面说明。
- 红黑双方历史时间。
- 范围进度条。
- 自动播放和暂停按钮及其定时器状态。
- 跳到第一步和跳到最后一步按钮。

信息视野和棋盘朝向控件继续保留，因为它们不属于步进控制。普通回放的分享链接生成与撤销区域也继续保留。

#### 3.10.2 布局与交互

- 精简控制条紧邻棋盘下方，不再使用占据大量高度的侧栏卡片。
- 排列顺序为“上一步、步数、下一步”；桌面和手机使用同一组件。
- 初始局面时禁用“上一步”，最后一步时禁用“下一步”。禁用按钮不改变当前帧，也不播放声音。
- 切换红黑或全局视野、翻转棋盘和网络重新获取回放数据时保持当前步数；只有回放 ID 变化才重置为第 0 步。
- 320 CSS 像素宽视口中控制条完整显示，不产生横向滚动，不保留删除内容形成的空白区域。

### 3.11 历史回放音效

#### 3.11.1 播放规则

普通历史、分享历史和管理员历史回放复用现有 `AudioService` 及同一套音量、静音、格式回退和自动播放解锁规则：

- 用户点击“下一步”并成功进入更大的帧索引时，根据目标帧的权威走棋播放声音。
- 普通走棋播放 `move`，吃子播放 `capture-chi`。
- 进入终局帧时播放终局声音；终局吃子沿用 3.1.2 的顺序，先播放“吃”，再播放终局声音。
- 点击“上一步”不播放声音，因为该操作是在回看旧局面，不代表重新发生一次走棋。
- 初次加载、刷新、切换信息视野、翻转棋盘和同一帧数据重新渲染不播放声音。
- 用户先回到上一步再点击“下一步”时，应再次听到该步声音；回放播放去重不能复用实时棋局永久按版本去重的键。
- 当前显示侧方视野时仍按该历史帧的真实动作类型发声，与实时棋局双方都能听到走棋和吃子的规则一致；不得通过声音之外的数据泄露隐藏坐标或棋子位置。

#### 3.11.2 实现边界

`AudioService` 增加显式回放播放入口，例如 `emitReplay`，使用“回放页面实例 + 向前步进序号 + 事件”作为一次导航内的去重键。它必须与实时棋局的 `gameId + version + event` 去重集合隔离，避免查看回放后抑制实时声音，也避免 HTTP 重渲染造成重复播放。

声音类型从目标帧的权威 `ReplayMoveView.Captured` 和终局状态确定，不从当前选中的红方或黑方投影中猜测。回放 API 不需要新增隐藏坐标字段；现有 `omniscient.move` 已具备判定普通走棋和吃子的必要信息。

## 4. API 契约

### 4.1 悔棋接口

新增：

```text
POST /api/games/{gameId}/takeback-requests
POST /api/games/{gameId}/takeback-requests/{requestId}/accept
POST /api/games/{gameId}/takeback-requests/{requestId}/reject
```

发起请求：

```json
{
  "expectedVersion": 12,
  "clientRequestId": "01J4THQ8V8J8F47B9KJ8M93V5M"
}
```

响应：

```json
{
  "id": "00000000-0000-0000-0000-000000000000",
  "status": "pending",
  "requestedBy": "red",
  "requestedPly": 7,
  "requestedAtVersion": 12,
  "resolvedAtVersion": null,
  "createdAt": "2026-07-31T00:00:00Z"
}
```

- 发起成功返回 `200 TakebackRequestView`。相同玩家、相同 `clientRequestId` 的重试返回同一结果。
- 同意成功返回更新后的 `GameView`。
- 拒绝成功返回最终 `TakebackRequestView`。
- 所有接口继续使用玩家认证、防跨站请求伪造和 `command` 限流策略。

主要错误码：

```text
GAME_FINISHED                 棋局已经结束
STALE_VERSION                 发起时局面版本已变化
TAKEBACK_NOT_AVAILABLE        最新有效走棋不属于请求者或当前不是对方回合
TAKEBACK_ALREADY_REQUESTED    同一手已经请求过悔棋
TAKEBACK_WINDOW_CLOSED        对方已经走棋、请求已关闭或目标走棋不再是最新一手
NEGOTIATION_PENDING           已有另一项求和或悔棋请求待处理
CANNOT_RESPOND_OWN_REQUEST    请求者尝试接受或拒绝自己的请求
```

### 4.2 棋局视图

`GameView` 增加：

```text
takebackRequest  TakebackRequestView | null
lastAction       GameActionView | null
```

`takebackRequest` 只返回当前待处理请求；终局或请求解决后为 `null`。`lastAction` 只包含声音和界面同步所需的动作类型、行为方和版本，不包含侧方视野本来不能看到的走棋坐标。

`DrawOfferView` 和 `TakebackRequestView` 在同一 `GameView` 中不得同时为 `Pending`。

### 4.3 实时通知

新增 SignalR 事件：

```text
TakebackRequestChanged(TakebackRequestView)
```

接受悔棋时先提交数据库事务，再发送请求状态和最新棋局通知。前端不能依赖两个通知的到达顺序：

- `GameView.Version` 决定棋局新旧。
- 请求实体的最终状态覆盖 `Pending`。
- 重连、页面恢复可见和通知丢失时，通过 `GET /api/games/{gameId}` 恢复完整权威状态。

## 5. 数据模型与迁移

### 5.1 悔棋请求表

新增 `takeback_requests`：

```text
id                          uuid                     primary key
game_id                     uuid                     not null
requested_by_player_id      uuid                     not null
move_id                     uuid                     not null
requested_ply               integer                  not null
requested_at_version        bigint                   not null
resolved_at_version         bigint                   null
client_request_id           varchar(64)              not null
status                      varchar(16)              not null
created_at                  timestamp with time zone not null
updated_at                  timestamp with time zone not null
```

状态：

```text
Pending | Accepted | Rejected | Withdrawn
```

约束与索引：

- `requested_ply > 0`、版本非负。
- `Accepted` 必须有 `resolved_at_version`；未解决状态的版本字段必须符合状态约束。
- `(game_id, requested_by_player_id, client_request_id)` 唯一，保证发起幂等。
- `move_id` 唯一，保证同一手最多请求一次。
- 每局最多一条 `Pending` 的部分唯一索引。
- 外键删除策略使用 `Restrict` 或随棋局级联，必须与现有棋局审计数据保留策略一致。

### 5.2 走棋撤销字段

在 `moves` 增加：

```text
reverted_at                    timestamp with time zone null
reverted_by_takeback_request_id uuid                    null
```

- 两个字段必须同时为空或同时有值。
- `reverted_by_takeback_request_id` 唯一，一次接受请求只能撤销一手。
- 将现有 `(game_id, ply)` 唯一索引改为只覆盖 `reverted_at IS NULL` 的部分唯一索引，使重新落子可以使用被撤销的 `ply`。
- 所有当前局面、历史、回放、总手数、重复局面辅助查询默认只读取 `reverted_at IS NULL` 的有效走棋。
- 管理员审计查询必须显式选择是否包含已撤销走棋，避免普通查询意外暴露。

### 5.3 权威动作摘要

为准确区分走棋、吃子和接受悔棋，可以在 `games` 持久化最近动作类型及动作方，也可以由当前有效走棋和已解决请求按 `GameVersion` 确定性投影。无论采用哪种方式，都必须满足：

- 普通读取不额外执行逐局全表扫描。
- `lastAction.version` 与产生该动作的棋局版本一致。
- 老数据迁移后允许 `lastAction = null`，不能在首次加载时补播历史声音。

### 5.4 迁移兼容性

第四阶段迁移必须在已有生产棋局和走棋记录上原地执行：

- 现有走棋的撤销字段回填为 `null`，全部继续视为有效走棋。
- 不重建、不清空 `games`、`moves`、评分或历史表。
- 先建立新字段和约束，再安全替换 `(game_id, ply)` 索引，避免迁移中出现无唯一约束窗口。
- 更新 Entity Framework Core 模型快照。

## 6. 服务端实现分层

建议新增或扩展以下职责：

- `TakebackService`：发起、接受、拒绝、并发校验、局面回退和计时规则。
- `GameService.MoveAsync`：提交走棋前在同一事务中撤销待处理求和与悔棋请求，并生成权威动作摘要。
- `GameService` 的认输和终局路径：统一撤销待处理协商请求。
- `GameViewProjector`：投影 `takebackRequest` 和 `lastAction`，不泄露侧方视野走棋坐标。
- `HistoryService`：所有回放和总手数查询过滤已撤销走棋；全局帧继续同时提供红、黑、全局三份投影。
- `IGameNotifier`：发送悔棋状态变化；通知失败不得回滚已经提交的数据库事务。

控制器只负责鉴权、参数绑定和调用应用服务。悔棋接受、走棋、超时扫描、认输和管理员判负必须复用现有棋局行锁及可串行化事务约定，避免局面、计时、终局或评分出现双重结果。

## 7. 前端实现分层

建议新增：

```text
apps/web/src/features/game/GameNegotiationOverlay.tsx
apps/web/src/features/game/TakebackRequestPanel.tsx
apps/web/src/features/game/PlayerClockBar.tsx
```

并扩展：

- `api/client.ts`、生成的 `schema.d.ts` 和 `types.ts`：增加悔棋接口与契约。
- `api/hubs.ts`：订阅 `TakebackRequestChanged`，继续用 HTTP 回取恢复丢失通知。
- `features/game/gameViewCache.ts`：相同棋局版本下也能合并求和、悔棋和计时状态；更高版本仍按权威版本替换。
- `GamePage.tsx`：移除内部页眉信息，增加悔棋命令、中央请求覆盖层、双方身份计时布局和己方回合状态。
- `GameBoard.tsx`：增加独立 `orientation`、己方回合视觉状态和全局红黑盲区覆盖数据。
- `ReplayPage.tsx`、`AdminReplayPage.tsx`：拆分 `visibilityMode` 与 `orientation`，向棋盘传递两方可见区域；移除进度条、局面说明、历史时间、自动播放、首步和末步跳转，只保留上一步、下一步与步数。
- `audioService.ts`：把 `capture` 映射到合成后的中文“吃”资源，按 `lastAction` 播放实时声音，并提供与实时去重隔离的历史回放播放入口。
- `index.css`、`admin.css`：请求覆盖层、回合呼吸边框、计时条、朝向控件和全局盲区样式。

中央请求覆盖层的状态应由 `GameView` 和请求通知归并得到。不得在多个组件各自维护一份无法校准的 `drawOffer` 或 `takebackRequest` 最终状态。

## 8. 安全、隐私与可访问性

- 悔棋接口只允许棋局参与者访问；通过修改 `gameId` 或 `requestId` 不能读取或响应其他棋局请求。
- 服务端根据认证玩家推导请求方和响应方，禁止客户端提交任意 `requestedBy`、`moveId` 或 `ply`。
- 红方和黑方实时视野仍只返回各自可见棋子。全局红黑盲区仅用于已经具备全局投影权限的回放，不得加入实时玩家视图。
- 请求 ID、客户端幂等 ID和棋局 ID可以进入结构化日志；游客令牌、Cookie 和完整棋局状态 JSON 不得写入日志。
- 中央请求卡片必须支持键盘焦点、可读标题、按钮禁用状态和屏幕阅读器状态通知。
- 边框动画遵守降低动态效果设置，并保持低频、非刺眼。
- 红黑盲区除颜色外还应通过纹理或图例区分，避免只依赖色觉。

## 9. 验证方案

### 9.1 服务端

- 只有最新有效走棋的行为方能在轮到对方时发起悔棋。
- 初始局面、自己的回合、对方已经走棋、终局和同一手已请求时均被拒绝。
- 相同 `clientRequestId` 重试发起不会创建重复记录。
- 接受第一手时从 `InitialStateJson` 恢复；接受后续手时从上一条有效走棋快照恢复。
- 接受后棋局版本严格增加，半回合数减少一，行棋方回到请求者。
- 被撤销走棋保留审计记录，但所有普通历史与回放只包含有效走棋。
- 拒绝后局面、版本和计时不回退，同一手不能再次请求。
- 对方走棋与发起悔棋并发：走棋先提交时请求失败或被撤销，不能跨过新走棋回退。
- 对方走棋与接受悔棋并发：任一提交顺序都只有一个成功的权威局面，另一个得到陈旧版本或窗口关闭错误。
- 接受悔棋与超时并发：只产生继续棋局或超时终局之一，不重复终局和评分结算。
- 待处理求和与悔棋互斥；走棋、认输、超时和管理员判负会撤销待处理请求。
- 请求者总时间不返还走棋耗时，原走棋增量被撤销；对方处理请求期间的耗时保留。
- 接受悔棋重复请求幂等，不重复标记走棋或增加版本。
- `GameView.lastAction` 对普通走棋、吃子和接受悔棋给出正确动作类型，且不暴露隐藏坐标。

### 9.2 前端

- `GamePage` 不再渲染 `LIVE GAME`、实时同步文本和局面版本，也不保留对应空白块。
- 双方客户端只在收到新权威 `move` 或 `capture` 动作时各播放一次；HTTP 和 SignalR 重复不重复播放。
- `takebackAccepted` 版本变化不会触发走棋或吃子声音。
- 吃子使用 `capture-chi.ogg`，不支持 Ogg 时回退 MP3，播放速度为 1。
- 求和与悔棋的收到、发出、接受、拒绝、撤销、终局和重连状态都正确显示在棋盘中央。
- 请求提交中不能重复触发其他命令，服务端错误后可以恢复操作。
- 只有己方可操作回合显示边框呼吸动画；对方回合、命令等待与终局停止。
- 降低动态效果媒体条件下使用静态高亮。
- 红方玩家和黑方玩家看到的计时器都满足“对手在上、自己在下”。
- 切换回放信息视野不会改变棋盘朝向；翻转棋盘不会改变信息视野或当前帧。
- 全局视野按同一帧红黑 `visibleSquares` 正确渲染红方盲区、黑方盲区、双方盲区和双方可见区。
- 红方和黑方侧方视野继续使用原有不透明未知区域，不泄露不可见棋子。
- 普通历史、分享历史和管理员历史的控制区只包含上一步、下一步与当前步数和总步数；局面说明、历史时间、范围进度条、自动播放、首步和末步跳转均不存在，也不残留空白。
- 初始步禁用上一步，末步禁用下一步；视野和朝向切换保持当前步数。
- 向前进入普通走棋帧播放一次走棋声，进入吃子帧播放一次“吃”，进入终局帧按规则播放终局声音。
- 初次加载、上一步、切换视野、翻转棋盘和同帧重渲染不播放声音；退回后再次向前允许重新播放该步声音。

### 9.3 真实浏览器与端到端场景

1. 玩家 A 走普通一步，确认 A 和 B 各听到一次走棋声，刷新 B 后不补播。
2. 玩家 A 吃子，确认 A 和 B 各听到一次清晰中文“吃”，不叠加普通落子声。
3. A 走棋后在 B 尚未走棋时请求悔棋，B 的桌面和 400 × 845 手机视口都立即在棋盘中央看到请求。
4. B 同意后，双方棋盘恢复到 A 走棋前，轮到 A，版本继续增加，双方计时符合不返还规则。
5. A 再次走棋并请求悔棋，B 拒绝；A 不能针对该手再次请求，棋局继续。
6. A 请求悔棋后 B 直接走棋；请求自动失效，A 不能撤销 B 已提交的棋。
7. A 请求悔棋与 B 几乎同时走棋，重复执行并发场景，确认每次只有一个权威结果且双方最终一致。
8. 在待处理求和时尝试发起悔棋，在待处理悔棋时尝试求和，确认界面和 API 都禁止重叠请求。
9. 红方和黑方分别进入计时棋局，确认对手计时器始终在棋盘上方，自己始终在棋盘下方；手机首屏可见完整关键区域。
10. 在普通回放和管理员回放中固定红方在下，依次切换红方、黑方、全局视野，确认棋盘不旋转；单独点击翻转后只改变朝向。
11. 在全局视野选择一个仅红方可见、仅黑方可见、双方可见和双方都不可见的方格，核对四种遮罩结果。
12. 在开启和关闭降低动态效果的系统设置下检查己方回合提示，确认动画和静态替代都清晰且不影响走棋。
13. 在普通历史、分享历史和管理员历史中核对控制条，确认只保留上一步、下一步和 `当前步数 / 总步数`，手机下没有被删除卡片留下的空白。
14. 依次向前查看普通走棋、吃子和终局帧，核对对应声音；再执行上一步、切换视野和翻转棋盘，确认这些操作静音，随后再次下一步可以重播该手声音。

## 10. 需求追踪

| 原始需求 | 本规格章节 |
| --- | --- |
| 1. 走棋声音对两边播放 | 3.1、4.2、9.1、9.2 |
| 2. 添加悔棋请求 | 3.2、4.1、5.1、5.2 |
| 3. 悔棋、求和在棋盘中间提示 | 3.3、7、9.2 |
| 4. 不显示实时同步和局面版本 | 3.4 |
| 5. 不显示 `LIVE GAME` 等红框内容 | 3.4 |
| 6. 己方回合棋盘边框闪烁 | 3.5 |
| 7. 视野切换不改变朝向，增加独立朝向控制 | 3.6 |
| 8. 全局视野显示红黑半透明不可见区域 | 3.7 |
| 9. 吃子增加显著中文“吃”语音 | 3.8 |
| 10. 对手计时在上、自己计时在下 | 3.9 |
| 11. 回放只保留上一步、下一步和步数 | 3.10、7、9.2 |
| 12. 历史回放播放声音 | 3.11、7、9.2 |

## 11. 完成定义

以下条件全部满足后，第四阶段才算完成：

- 每次已提交普通走棋和吃子都在双方客户端各播放一次，重复响应、重连和悔棋版本变化不会误播。
- 吃子使用授权清楚、手机可辨认的中文“吃”语音与重击合成资源。
- 悔棋只允许在自己上一手后、对方尚未走棋的窗口发起；接受、拒绝、直接走棋、超时和终局并发结果正确且幂等。
- 接受悔棋只撤销一手，版本保持单调递增，计时不返还，历史、重复局面和无进展状态全部与有效走棋一致。
- 求和和悔棋请求在桌面及手机上都于棋盘中央显著显示，并可从断线重连恢复。
- 实时对局页不再显示 `LIVE GAME`、实时同步状态和局面版本，也不残留占位空白。
- 己方回合边框提示清晰、低频，并提供降低动态效果的静态替代。
- 普通回放与管理员回放的信息视野和棋盘朝向完全解耦，全局视野正确显示红黑双方盲区。
- 红方和黑方玩家的实时计时器均按对手在上、自己在下排列，400 × 845 手机竖屏关键内容可见且无横向滚动。
- 普通历史、分享历史和管理员历史回放只保留上一步、下一步、当前步数和总步数，删除的进度、状态、时间与跳转控件不残留空间。
- 历史回放向前步进会播放正确的走棋、中文“吃”和终局声音；后退、初载、视野切换与棋盘翻转保持静音。
- 第四阶段数据库迁移可保留现有生产棋局、走棋、评分和历史数据执行。
- 服务端、前端和端到端验证全部通过，并完成双方真实浏览器音频、并发悔棋、回放切换和手机竖屏检查。
